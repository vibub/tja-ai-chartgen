# Examples

Place local test audio files here when manually trying the CLI.

Example:

```bash
tja-ai-chartgen generate examples/song.mp3 --title "Example Song"
```

Audio files are intentionally not committed by default. Generated files are written to `output/` unless `--output-dir` is provided.
