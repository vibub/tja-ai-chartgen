# AGENTS.md

This file provides guidance to Codex (Codex.ai/code) when working with code in this repository.

## 项目定位

`tja-ai-chartgen` 是一个持续迭代的太鼓达人 `.tja` 谱面生成项目。目标流程：

1. 输入音频文件。
2. 使用音频分析工具提取 BPM、拍号、节拍点、结构段落等音乐信息。
3. 将结构化分析结果交给 AI。
4. 生成可维护、可校验、可逐步改进的 `.tja` 谱面。

当前项目使用 Python 3.11+，以 Typer CLI 作为入口，目标是先完成可安装、可测试、可生成规则谱面草稿的 MVP。后续每次引入工程结构、运行命令、外部工具链、核心模块或重要约束时，都应同步更新本文件。

## 当前开发命令

```bash
pip install -e ".[dev]"
pip install -e ".[dev,instrument]"
tja-ai-chartgen prepare-instrument-models
tja-ai-chartgen prepare-instrument-models --profile full
tja-ai-chartgen version
pytest
pytest tests/test_tja_writer.py -v
pytest tests/test_audio_pipeline_integration.py -v
pytest tests/test_audio_fixture_ground_truth.py -v
pytest tests/test_phase_two_constraints.py -v
pytest -m instrument_model -v
python tests/fixtures/audio/rebuild_click_fixtures.py
python tests/fixtures/audio/rebuild_click_fixtures.py --ground-truth-only
python tests/fixtures/audio/rebuild_click_fixtures.py --fixture syncopated_120.wav
python tools/benchmark_audio_fixtures.py
python tools/benchmark_chart_alignment.py
python tools/benchmark_instrument_models.py --profile stem-role --device auto
python tools/benchmark_instrument_models.py --profile full --device auto
python tools/verify_phase_zero.py
python tools/verify_phase_one.py
python tools/verify_phase_two.py
python tools/verify_phase_three.py
python tools/verify_phase_four.py
python tools/verify_phase_five.py
python tools/verify_phase_six.py
python tools/verify_phase_seven.py
python tools/analyze_reference_dataset.py <reference-dir>
python tools/build_reference_windows.py <reference-dir> --output src/tja_ai_chartgen/ai/reference_windows.json
ruff check .
```

生成规则谱面草稿的目标命令：

```bash
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title"
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --max-bars 16
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --density high --style hybrid
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --density high --style performance --special-notes
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --all-courses
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --max-bars 16 --bpm 220.588 --offset 0.725
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --time-signature 3/4
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --use-beatnet
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --use-instrument-analysis --instrument-device auto
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --use-instrument-analysis --instrument-profile stem-role
tja-ai-chartgen generate path/to/song.mp3 --title "Song Title" --use-ai --model openai/custom-model --ai-base-url https://llm.example.com/v1 --ai-request-timeout 300 --ai-transport-retries 1
tja-ai-chartgen generate-from-config output/generation_config.json
tja-ai-chartgen web
tja-ai-chartgen web --host 0.0.0.0 --allow-remote
tja-ai-chartgen web --host 0.0.0.0 --allow-remote --allow-instrument-analysis
```

系统依赖：`ffmpeg`，用于把输入音频转换为 `.ogg`。Web UI 使用 FastAPI / Uvicorn / python-multipart，BeatNet 节拍增强使用默认依赖 BeatNet；受支持的平台同时安装带 Python 3.11–3.13 预编译 wheel 的 `madmom-prebuilt`，运行时兼容其 `madmom` 模块与发行版名称差异，避免原版 madmom 在现代 Python/Windows 上本地编译失败。可选阶段 C 通过 `.[instrument]` 安装 PyTorch、Demucs、Transformers 与 Hugging Face Hub；`prepare-instrument-models` 是唯一允许下载权重的入口，默认推荐 `stem-role` profile，只准备 htdemucs 并写入项目根目录 `models/stem-role-v1/`；显式 `--profile full` 才准备 Demucs + AST 并写入 `models/instrument-v1/`，整个 `models/` 目录受 `.gitignore` 保护，生成过程只允许本地离线加载。统一 `instrument_models.json` manifest 通过 `profile` 区分 `full` 与 `stem-role`，后者不要求 AST，并分别记录模型代码许可证、权重许可证、固定 revision 与文件 SHA-256；旧 `instrument-v1` manifest 继续兼容。`tools/benchmark_instrument_models.py` 对指定 profile 执行 manifest 全文件 hash 校验，在阻断 socket 且强制 Hugging Face/Transformers 离线环境中运行一次真实分析，并把模型总大小/组件大小、请求与实际设备、分析时长、墙钟耗时、real-time factor、进程峰值 RSS 及 stem/classifier 输出数量写入 `output/instrument_benchmark_<profile>.json` 和 `.md`；性能数据为环境相关的 report-only 诊断，不设跨机器硬阈值。`tests/fixtures/audio/` 保存 17 个项目代码生成的 WAV：基础 4/4 点击轨、sparse/dense、首部瞬态毛刺、直拍/三连音/混合 resolution、structure build-up，以及切分、真正弱起、低频主拍/高频反拍、持续 harmonic 背景加稀疏瞬态、短 fill burst、3/4、6/8 和半速/倍速歧义模式；`rebuild_click_fixtures.py` 只依赖 Python 标准库并从同一组 `FixtureSpec` 确定性重建 WAV、`ground_truth.schema.json` 和每个音频对应的 `<stem>.events.json`，事件 schema version 1 记录 BPM、拍号、时长、onset、强 onset、beat/downbeat、静音区、频带事件、fill 区间和结构段落；`--ground-truth-only` 可只刷新 JSON，`--output-dir` 可用于临时目录重建校验，重复传入 `--fixture <name.wav>` 可只重建指定 fixture，不包含第三方录音。`tools/benchmark_audio_fixtures.py` 使用 `audio-alignment-v2` 在 50 ms onset、70 ms beat/downbeat 默认容差和 0.25 频带峰值阈值下离线计算一对一事件匹配、BPM/拍号、first downbeat、静音误检、low/high band、pickup、fill 和 ResolutionPlan 指标，并将全部 17 个 fixture 的 schema version 2 基线及逐 fixture 报告分别写入 `audio_benchmark_baseline.json` 和 `audio_benchmark_baseline.md`；默认分析没有独立 downbeat 时按 offset、四分音符 BPM 和拍号推导并标记来源，`--use-beatnet` 可显式评测 BeatNet。`tools/benchmark_chart_alignment.py` 使用 `chart-alignment-v1` 对同一批 fixture 的 Easy 3、Normal 5、Hard 7、Oni 10 确定性规则谱面评测 note/onset、强 onset、downbeat、无证据 note、静音违规、fill 响应与重复生成确定性，并写入 `chart_alignment_baseline.json` 和 `chart_alignment_baseline.md`。`tools/verify_phase_zero.py` 执行 `phase-zero-acceptance-v1`：在两个临时目录中确定性重建全部 35 个 fixture/ground-truth 产物，在禁用 socket 连接时分别运行两次音频与谱面对齐 benchmark，验证 schema/覆盖范围/同环境稳定性，并扫描 baseline 中的真实歌曲身份、绝对路径、URL 和服务秘密；结果写入 `phase_zero_acceptance.json` 和 `phase_zero_acceptance.md`。`tools/verify_phase_one.py` 执行 `phase-one-acceptance-v1`：在禁用 socket 连接时对全部 17 个 fixture 独立运行两次 librosa/onset-grid、canonical `BarFeature`、`structure-v1` 和完整 hit/accent/don-ka salience，验证 canonical grid、确定性、首尾静音、70 ms onset 峰值对齐、confidence/reason 契约和稀疏输出；阻断门槛为 peak precision 不低于 0.98、recall 不低于 0.95、稀疏点比例不高于 0.50，结果写入 `phase_one_acceptance.json` 和 `phase_one_acceptance.md`。`tools/verify_phase_four.py` 执行 `phase-four-acceptance-v1`：在禁用 socket 连接时双跑稳定 4/4、3/4、6/8、真正弱起、半速/倍速歧义和弱节奏 6 个聚焦 fixture 的默认/BeatNet 分析，同时运行候选拒绝、完整选择、meter/downbeat 部分采用、ambiguity、手动覆盖和变速分类行为矩阵；验收要求 BPM/beat/downbeat/pickup 不退化、无 half/double tempo 错误、每个结果恰有一个最终候选、完整持久化 `tempo-arbitration-v3` / `tempo-variation-v1` 诊断，并验证默认路径不加载 BeatNet。结果写入 `phase_four_acceptance.json` 和 `.md`；当前 6 个真实 BeatNet fixture 均被保守拒绝，故 BeatNet 继续默认关闭。真实音频集成测试覆盖 ffmpeg 转换、librosa/onset-grid 与原始 RMS 分析、canonical `BarFeature`、`structure-v1`、16/24/48 与 12/18/36 resolution 规划、首尾静音保护、四难度规则生成、`QualityReport`、结构化 preflight 和 TJA 渲染/CP932 写入。CI 在 Ubuntu Python 3.11/3.13 运行完整测试，并在 Windows Python 3.13 运行真实 ffmpeg 音频流水线 smoke；Windows 通过固定到提交 `36c6454b5a2348e7794ba2d82a21506605921e3d` 的 `FedericoCarboni/setup-ffmpeg` v3 action 安装 ffmpeg，避免依赖 Chocolatey 社区源的临时可用性。

不要在本文件中保留已经失效的命令；命令变更时必须同步更新。

## 当前架构

项目围绕“可替换的流水线”组织，而不是把音频分析、AI 提示词、谱面输出耦合在单个脚本中：

- **CLI 编排层**：`src/tja_ai_chartgen/cli.py` 提供 `version`、`prepare-instrument-models`、`generate`、`generate-from-config` 和 `web` 命令，负责 Typer 参数、全难度循环、路径命名、TJA 渲染/写入、report 和终端输出；`--all-courses` 会使用 Easy 3 / Normal 5 / Hard 7 / Oni 10 内置等级输出四个独立 `.tja`，四张谱面共享用户选择的 density 偏移；阶段 C 通过 `--use-instrument-analysis` 显式启用，新 CLI/Web 任务默认使用推荐的 `stem-role`（仅 htdemucs 声部节奏增强），显式 `--instrument-profile full` 才运行旧 Demucs + AST 具体乐器分类兼容诊断；旧 GenerationConfig 缺少 profile 时仍兼容回到 `full`。设备可选 auto/cpu/cuda/mps，未启用时不导入重型模型。
- **共享生成用例层**：`generation.py` 定义带 `schema_version=1` 的 Pydantic `GenerationConfig`、结构化 `GenerationNotice` 和 `ChartGenerationResult`，并通过 `build_song_analysis()` 统一音频转换、分析 override、可选 `instrument-v1`、`spectral-v1`、canonical `BarFeature`、`structure-v1`、phrase plan 与 `ResolutionPlan` 构建，通过 `generate_chart_bars()` 统一 AI sidecar、事件编码、规则 fallback、通知和 `QualityReport` 生成；`build_analysis_notices()` 会把 BeatNet 未生效、BeatNet meter/downbeat 部分采用、疑似变速与固定 BPM 限制、频谱增强失败、`instrument-v1` / `stem-role-v1` 成功、classifier-only 降级、旧 partial 兼容、依赖或模型或设备 fallback、onset-grid 拒绝、低结构置信度和基础 resolution 降级转成可持久化提示。CLI 与 Web 只传入各自决定的路径和运行时 AI 连接信息，API key 与 base URL 不属于持久化配置字段，Web 进度、页面、任务目录和 CLI 报告仍由适配器独立维护。
- **音频层**：`audio/convert.py` 通过 `ffmpeg` 转 `.ogg`；`audio/analyze.py` 默认通过 `librosa` 提取 onset、RMS、duration 和初步 tempo，同一次 RMS 计算同时保留全曲归一化 activity 与仅在 raw analysis 中使用的原始 RMS envelope，再借鉴 ArrowVortex 的思路按 onset 在候选 beat 周期上的相位聚集程度重估 BPM 与 offset；librosa、librosa+onset-grid、BeatNet、BeatNet+onset-grid 统一写成 `TempoMeterCandidate`，记录各自 BPM、offset、拍号、beat/downbeat、已有 onset 支持、覆盖率、interval stability、置信度和候选自身接受状态；嵌套 `TempoMeterEvidence` 进一步保存有效 onset/次佳差距、interval 均值与变异系数、BeatNet beat number 完整性、meter cycle 稳定性、downbeat onset/低频/percussive 支持、小节长度合理性和 half/double tempo 解释力，启用 `instrument-v1` 时只追加 drum/bass downbeat 辅助支持而不改变候选结论；`tempo-arbitration-v3` 现按来源自适应权重计算 onset、覆盖率、interval、候选差距及 BeatNet beat number/meter/downbeat 分项，拒绝 tracker 不完整、不稳定、小节长度不合理、onset 明显弱于基础结果或总分不足的候选，并在结论冲突且分差不足或 onset-grid 自身歧义时保守回退调用 BeatNet 前的基础候选；当完整 BeatNet 候选未胜出但其四分音符 BPM 与基础结果一致，且 tracker、meter、downbeat 证据全部可靠时，保留基础 BPM 并仅采用 BeatNet 拍号、downbeat 与小节相位，BPM 冲突、弱 downbeat 或 ambiguity 均禁止部分采用；每个候选持久化 score components、最终 confidence、资格与选中状态，`TempoAnalysisDecision` 记录最终/次佳来源与分数、分差、拒绝映射、ambiguity，以及 `tempo_source`、`meter_source` 和 `partial_adoption`；候选证据同时记录固定 BPM 理论 interval、平均/P95/最大拟合误差、归一化误差、interval 异常比例、首尾漂移和最大相邻跳变；固定 BPM 拟合会把 tracker beat 单调投影到最近理论 beat index，允许漏拍或重复拍而不把后续全部 beat 错位，并要求相邻 interval 跳变同时具有足够异常 interval 比例才判定局部变速，避免单个 tracker 毛刺产生变速误报；歌曲级 `tempo-variation-v1` 优先使用具备足够 interval/覆盖率的原始 BeatNet tracker、否则使用 librosa tracker，区分 stable、possible-tempo-change、possible-rubato、possible-live-performance 与 insufficient-evidence，疑似变化只产生 warning 并标记固定 BPM 限制，不改变当前固定 BPM 输出；`audio/spectral.py` 的 `spectral-v1` 使用 HPSS 分离 harmonic/percussive waveform，并以同一 hop length 提取低/中/高频 onset、spectral flux、归一化 brightness、harmonic chroma novelty、brightness/flatness/percussive balance texture novelty 和 percussive ratio，所有 envelope 对齐并限制到 0–1，数字静音返回全零 complete，短音频、无效参数或 librosa 异常返回稳定 fallback 而不打断基础分析；`audio/instrument_models.py` 按 profile 解析项目根目录 `models/instrument-v1/` 或 `models/stem-role-v1/`、校验统一 manifest，并只在显式准备命令中下载固定 Demucs/AST revision；`stem-role` profile 仅准备和离线校验 htdemucs，不导入或要求 AST，`full` profile 与旧 manifest 继续兼容。`audio/instruments.py` 的 `instrument-v1` full 路径使用本地 `htdemucs` 分离 vocals/drums/bass/other，提取 stem 活动与 onset，再用本地 AST AudioSet 对 4 秒窗口、2 秒 hop 的 mix/other 分类为固定乐器 taxonomy；`stem-role-v1` 路径在 stem frame 成功后直接以 complete 返回，classifier model 为 `None`；full profile 的 Demucs 与 AST 均成功时继续输出兼容的 `instrument-v1 complete`，AST 文件缺失、Transformers 依赖/加载或推理失败时只要 Demucs 组件可用，就降级为 `stem-role-v1 complete`、保留 classifier failure reason 并跳过具体乐器 taxonomy，只有没有 stem 证据时才 fallback；partial 仅保留给真实部分 stem/后处理失败及旧 `instrument-v1 partial` JSON 兼容读取，旧 feature version/status/reason 不在读取时改写。两条路径均支持 CUDA OOM 有限重试和完全离线加载；候选节拍评分会按有效 onset 总权重归一化，并根据有效 onset 数、时间跨度、覆盖率、支持率和独立次佳候选差距决定是否接受；支持率门槛会随有效 onset 数与时间覆盖率平滑放宽到高证据下限，减少密集复杂音乐中大量有效证据被固定阈值误拒绝，同时随机、稀疏、短覆盖或候选模糊时仍保留 librosa 基线；接受后才使用加权圆形均值和 waveform 上升沿支持精调 offset，并生成可保留负 offset 起点的规则 beat grid；BeatNet 已纳入默认项目依赖，但仅在启用 `--use-beatnet` 时加载；分析前会兼容 `madmom-prebuilt` 的发行版名称、补齐 NumPy 1.24+ 移除的旧类型别名，并为非流式分析屏蔽不需要的 PyAudio 导入，再取得 beat number、downbeat 和 meter，并用同一套 onset-grid 置信度门控校正 BPM/offset，低置信度时保留 BeatNet 基线；内部与 TJA 的 BPM 始终表示四分音符 BPM，BeatNet 判定 6/8 且 `1..6` 行间隔表示八分音符时会先换算为等价四分音符 BPM，并防止 onset-grid 再次选择双倍速度；BeatNet 导入、推理、输出解析或合并失败时均保留此前的 librosa 分析结果。
- **特征层**：`features/meter.py` 定义 `4/4`、`3/4`、`6/8` 的小节网格和 `#MEASURE` 映射；`features/salience.py` 固定统一节奏显著性契约版本 `rhythmic-salience-v1`，并通过 `build_canonical_rhythmic_evidence()` 把旧字段和显式 grid feature 中的 onset/strength、activity、beat/downbeat、稀疏频带攻击、spectral flux，以及 vocal/drum/bass/accompaniment onset 与对应 bar-level stem activity 确定性对齐到完整 canonical grid；该统一证据视图会按通道取最大值、忽略越界格点并兼容 4/4、3/4、6/8；stem onset 统一受对应 activity 门控，drum onset 可作为 hit/accent/burst 瞬态证据，与 mix/spectral 重合时提高强度和 confidence、冲突时限制为中等 hit；不属于稳定直拍/三连音子网格的 stem-only drum 微时差会被拒绝，避免 Demucs 相邻峰与分离时延独立制造 hit；bass onset 只弱增强 beat/downbeat 与 don preference，vocal onset 只修正 phrase/cadence 首个已有 hit，accompaniment onset 只修正 highlight 上的已有候选，后三者均不独立制造 hit；同模块的 hit salience 在此基础上以 onset strength 和局部 spectral 峰值作为瞬态主证据，以受 activity/energy 门控的 beat/downbeat 作为无瞬态骨架，并仅用 structure role 调整已有候选，不允许 activity 或 structure 单独制造 hit；相邻 spectral frame 做局部峰值合并，显式连续 onset 保留，重复派生证据取最大值而不累加，activity 派生的 grid strength 不会被误判为 onset，数字静音和首尾静音强制归零；onset、spectral 与 beat drive 另有绝对门控，逐点置信度融合瞬态强度、全曲 75th percentile 参考、局部峰值差距、activity 支持和多证据一致性，小节置信度结合有效事件数、瞬态占比与活动覆盖，并以 `edge-silence`、`silent-bar`、`no-rhythmic-evidence`、`beat-skeleton-only`、`low-confidence` 稳定 reason 标记不可直接消费的结果；accent salience 只在已有 hit 候选上融合 downbeat、达到绝对强度门槛的 onset 局部峰值、低频攻击、旧 accent hint、歌曲/段落/乐句起点、正向 energy delta 和 peak/cadence/fill 等结构修正，downbeat 与强 onset 重合时显式提高 accent，不独立制造 hit，也不直接决定大音符；don/ka preference 继续只作用于已有 hit/accent 点，达到绝对频带攻击门槛的低频主导与 downbeat 弱偏咚，高频主导先提供基础咔倾向，再由 percussive ratio 和受限 brightness 证据增益，brightness 不允许单独制造咔偏好；反拍只提供弱咔位置提示，中频主导或频带混合保持中性，倾向值设上限，最终配色仍由 style 和生成器约束；连续同色证据不再按串长软化或强制改写；同模块新增 bar-level burst salience，在完整 hit/accent/don-ka 流水线后检测后半小节 onset 密度跃升、spectral flux 爆发、percussive ratio 上升和前段稳定对比，输出 0–1 score/confidence、canonical 起止格和稳定 reason；phrase end/cadence 及后继 section/peak/drop 只能增强已经通过纯节奏门控的 burst，不能单独制造候选，首尾静音和普通 phrase end 保持零值；`features/salience_candidates.py` 复用完整 salience 流水线，按可靠强瞬态（含活动门控后的 drum stem onset）、普通瞬态、持续 beat/downbeat 骨架、结构 highlight 和弱证据建立确定性候选顺序，并按逐小节 `ResolutionPlan` 仅保留无需量化即可表达的 canonical 点，同时移除越界与同格重复候选；候选层已接入 fallback 普通 note 与重音选择，统一重音候选按 accent、证据可靠性和确定性次序排序，规则生成会先消费可靠候选，再在 course 相关的总弱补点预算内选择仍有 onset/beat/activity/spectral/instrument 支持的格点，最后才在更小的无证据预算内补 style 骨架；连续非可靠 note 最多 2 个，sparse/breakdown 不允许无证据补点，无格点证据的活跃小节只保留 course 最小骨架；AI prompt 已通过 `compact-v7` 消费同一批可表达 salience 候选及可靠 burst score/confidence/可表达起止格，并为成功 AI 结果持久化首轮 report-only 对齐诊断；分析持久化仍留给后续阶段；`features/bars.py` 将 raw analysis 映射为每四分音符 12 tick 的 canonical `BarFeature`（4/4 为 48 格，3/4 和 6/8 为 36 格），onset、activity 和 spectral frame 会先量化到相对分析起点的全曲绝对格点再拆分到对应小节，越界事件被丢弃；onset/strength 保留瞬态检测结果，`activity_grids` 保留归一化 RMS 持续音乐活动，频谱小节字段保存低/中/高频 onset 峰值、spectral flux、brightness、harmonic/texture novelty 与 percussive ratio，`spectral_grid_features` 只保留不低于 0.05 的稀疏多频带攻击/flux；`features/instruments.py` 将 stem frame 和 AST 窗口按时间重叠映射成 `InstrumentBarFeature` 与稀疏 `InstrumentGridFeature`，记录人声 presence、鼓/贝斯/伴奏活动、固定乐器分数、主导声部/乐器与置信度，首尾静音小节强制归零；旧 `onset_16` / `accent_16` / `activity_16` JSON 仍可兼容读取；原始 RMS 帧按小节时间边界汇总为 `rms_dbfs`、`peak_rms_dbfs`、`relative_rms_db` 和 `sustained_activity_ratio`，beat/downbeat 使用 canonical 网格上的稳定理论主拍（4/4 为 `0/12/24/36`、3/4 为 `0/12/24`、6/8 复拍子为 `0/18`）；`features/structure.py` 的 `structure-v1` 在构建结构向量前消费统一 hit/accent salience，以 canonical primary onset evidence 和 salience point 生成 onset 密度/强度、accent 密度及 12 维 rhythm profile，并结合 energy percentile、持续 activity、前后窗口变化、timbre 差异和 novelty 选择非固定长度乐句；可选 stem-role 的受门控 drum onset 通过 salience 参与节奏趋势，声部结构上下文只读取 vocals/drums/bass/other activity 与粗粒度 dominant source，具体乐器 taxonomy 不参与边界、section 或 transition role 决策；同时以 spectral trend/balance、人声进出、主导声部切换和合奏趋势补强 build-up、peak、breakdown、cadence 与 fill，输出 boundary confidence、`phrase_id` / progress / position、可复用 `section_id`、intro/verse/chorus/break/outro 语义、`stable` / `build_up` / `peak` / `drop` / `cadence` / `breakdown` 角色与 `fill_candidate_score`，4/8 小节只提供很弱的长度先验，`features/sections.py` 仅保留兼容入口；`features/resolution.py` 先按全曲证据选择 16/24/48 或 12/18/36，默认 `song-global-v1`，只有完整高置信乐句存在明显 off-grid onset 时才使用 `phrase-stable-v2` 在乐句边界整段升级，相同 `section_id` 复用不低于首次出现的 resolution，切换过多则回退全曲稳定高 resolution；当 16/24/48 或 12/18/36 全部超过 0.2 tick 误差门槛时，不再默认选择最高 resolution，而是在最佳误差 0.05 tick 范围内选择最低稳定 resolution；`features/silence.py` 统一识别歌曲开头和结尾的连续静音小节，并仅在首尾连续扫描中结合绝对/相对响度、峰值、持续帧比例和 onset 数忽略孤立低电平毛刺；`features/density.py` 基于 energy、onset、activity、section、乐句进度和 transition role 为每小节生成 silent / rest / sparse / normal / dense / fill 密度提示，min/max 仍是硬约束，`target_scale` 只在当前 course/level、speed cap 和 occupancy cap 内调整目标，低 fill score 的 phrase end 不会被机械升级为 fill。
- **数据模型层**：`tja/model.py` 定义 `SongAnalysis`、`TempoMeterCandidate`、`TempoMeterEvidence`、`TempoAnalysisDecision`、`TempoVariationDiagnostic`、`ResolutionPlan`、`GridFeature`、稀疏 `SpectralGridFeature`、`InstrumentGridFeature`、`RhythmicSaliencePoint`、`BarRhythmicSalience`、嵌套 `InstrumentBarFeature`、`BarFeature`、`BarStructureFeature`、`PhraseFeature`、稀疏 `ChartBarEvents`、`ChartMetadata`、`ChartBar`、`TjaChart`，是 JSON 输出、AI 输入和 TJA writer 之间的稳定中间表示；节奏显著性点统一使用 canonical 非负格点，hit/accent/don/ka/activity/confidence 均限制为 0–1，并以短 reason code 保留可解释证据；`BarRhythmicSalience` 另以兼容默认值保存 bar-level `burst_score`、`burst_confidence`、`burst_start_grid` / `burst_end_grid` 和 `burst_reasons`，范围使用 canonical grid，当前不改变 `analysis.json` schema；`BarFeature` 的响度、持续性、频谱和结构诊断字段均提供兼容默认值，旧分析和直接 TJA 预览仍可读取；当前自动分析写入 `analysis_schema_version=11`、统一 `tempo_candidates`、BeatNet 状态/reason、`spectral_feature_version=spectral-v1` 及状态/reason、`instrument_feature_version=instrument-v1` 及状态/reason、模型 ID/实际设备、`structure_feature_version=structure-v1`、structure confidence、逐小节结构、phrase plan、resolution policy/plan、最终 analyzer、每小节响度/频谱诊断，以及 onset-grid 提议值、支持率、有效 onset 数、时间覆盖率、次佳候选、接受状态和决策原因。
- **谱面生成层**：`rules/styles.py` 定义 technical / stamina / hybrid / performance 的节奏倾向、咚咔配色和特殊音符门槛模板；`rules/fallback_generator.py` 先在 canonical tick 上生成普通/长音事件，再由 `tja/event_encoder.py` 按 `ResolutionPlan` 确定性编码 notes；每次 fallback 生成只构建一次 don-ka 或完整 burst salience，同一批 `BarRhythmicSalience` 同时供普通落点候选、performance accent 大音符、don/ka 配色与特殊音符范围消费，候选层可接收预计算 salience 并验证 bar 数一致，避免普通与特殊路径重复分析后产生证据漂移；显式使用 course / level 建立按小节时长归一化的 notes/sec 难度基线，该基线按 5 组匿名真实四难度谱面聚合范围做初步校准，density 只以 0.75–1.20 的有限乘数偏移。普通落点按 density hint、energy、onset 丰富度和 transition role 调节目标 hit 数，并在逐小节 `ResolutionPlan` 过滤后优先消费可靠 salience 强瞬态、普通瞬态、持续 beat/downbeat 骨架和结构 highlight，候选不足时先在 Easy/Normal/Hard/Oni 对应的弱证据预算内使用旧 style/spectral 评分和受统一 salience 门控的 stem 修正，并将无 onset/beat/activity/spectral 或可靠 drum stem 支持的 style 骨架限制在更小子预算内；非 drum stem onset 不再绕过 salience 直接成为独立格点证据；所有非可靠补点最多连续 2 个，优先连接可靠节奏，sparse/breakdown 不补无证据点，纯活跃无格点证据时只保留 course 最小骨架；稀疏 spectral attack/flux 仍作为额外落点证据；最终配色不再直接把频带映射为 note 字符，而是以 style 配色作为基础票，再融合统一 salience 的 don/ka preference 和弱 bass 证据，只有倾向分超过明确差距才覆盖 style，证据不足时保持 motif；普通 `1`/`2` 配色不再按连续同色串长度强制翻转，最终 note 保留 style 与音频证据共同决定的颜色；`longest_monochrome_run` 仅作为 report-only 诊断；启用阶段 C 时 drum onset 提供较强落点证据，bass onset 提供较弱主拍/咚偏好，vocal onset 只轻微支持乐句位置，伴奏 onset 支持 motif/highlight，所有声部与乐器信息仍服从难度和可玩性硬约束；performance 风格会把统一 accent salience 候选映射为少量大音符，Easy/Normal 每小节最多 1 个、Hard/Oni 最多 2 个；大音符必须与前后任意 `1`–`4` 普通击打保持超过 0.25 秒的真实时间间隔，该隔离规则跨小节且不依赖 16/24/48 或 12/18/36 编码长度，因此快速歌曲中的 `102`、`1002` 等左右手交替双击保持普通 `1`/`2`，不会升级为大咚/大咔；其他落点保持普通音符，避免 style 模板绕过重音预算；build-up 随 phrase progress 渐进，peak 提升普通 hit/重音而不是堆大音符，breakdown 保留骨架；silent/rest/sparse 保持严格上限，所有小节仍受最大 notes/sec 和格点占用率限制。`--special-notes` 只在可靠 burst salience 通过统一 score/confidence 门控后生成单小节滚奏或气球；burst canonical 起止格先精确投影到当前 `ResolutionPlan`，特殊音符必须位于该范围且实际持续时间不少于 0.25 秒，结构 `fill_candidate` 只能增强气球等类型选择，不能替代纯节奏 burst，phrase/song end 本身也不足以触发；持续秒数按实际跨度计算，气球击打数按 course / level 计算。`ai/reference_windows.json` 当前由 5 组本地参考、23 个 course 离线生成 69 个匿名连续窗口，运行时 `ai/reference_windows.py` 按 course/近似 level 选择 intro、peak、cadence，外部参考目录不参与运行时；窗口不可读时才回退到旧 `ai/examples.py` 均匀抽样片段。`rules/rhythm_skeleton.py` 复用确定性 fallback 落点选择生成 raw skeleton，再按完整 phrase 对全部可表达的可靠瞬态候选加权评估带相位的直拍、三连音/24 分与保守 mixed lattice；无可靠瞬态时才使用有持续活动支持的 beat/downbeat 候选，规则草稿的补点不参与 lattice 推断，生成 `rhythm-skeleton-v5` musical skeleton；lattice 内 tick 原样保留，lattice 外 tick 不做邻近投影，只有 confidence 不低于 0.95、同一精确 tick 至少重复 3 次的强瞬态可在每 phrase 10% 预算内作为例外，其余孤立微时差移除；density hint 仅允许用同一 lattice 内尚未选择的可靠候选补足最低 hit 数，且不得超过原始 course/level hit 预算，证据不足时允许少于最低密度，不恢复已拒绝的微时差；`ai/prompts.py` 通过 `tja-ai-chartgen-compact-v7` 紧凑 payload 一并传递 canonical grid、目标 resolution、beat、骨架和只含 vocal/drum/bass/other 粗粒度角色的 `bar_instruments`；其中 dominant source 与 confidence 在 payload 构建时仅由四类 activity 重新派生，不读取 `dominant_instrument` 或可能被 AST taxonomy 提高的旧混合 confidence，因此具体乐器字段变化不会改变当前 AI 输入；同时传递频谱小节汇总、density hint、逐小节结构、phrase plan、参考窗口，以及按目标 resolution 精确过滤后的稀疏 `bar_salience`；salience point 以 0–1000 整数传递 hit/accent/don/ka/activity/confidence 和证据等级，prompt 要求优先可靠 salience、限制无证据连接点，并只把 instrument 语义用于乐句、motif 和段落上下文而不直接决定 note tick；AI salience 每小节最多发送 16 点，低置信最高 resolution 不发送任意 finest-grid 微时差，高置信时每小节也最多保留 1 个高置信 onset 例外；prompt 要求乐句维持统一直拍或三连音子网格，不复制 `5/7` 间隔抖动和 `12/13` 相邻 stem 峰；每小节其他音频通道仍先投影到可演奏格点，避免发送 AI 无法落点的 canonical 细分和重复 grid 坐标。`ai/client.py` 提供 LiteLLM/OpenAI 兼容调用、严格事件 schema、自动修复和质量门控；`ai/sidecars.py` 为历史与当前 AI input/output/attempt sidecar 提供只验证 UTF-8 JSON 对象边界的兼容读取，原样保留 compact-v4 具体乐器列、旧 notes 输出及未知字段，不执行静默 schema 迁移；成功 AI 结果会额外生成 `ai-salience-validation-v1` 首轮 report-only 诊断，记录可靠候选与强瞬态覆盖、无 salience 支持 note 比例、首尾静音违规、可表达 note 数和有限示例，该诊断本身仍只持久化到 AI output/attempt sidecar，不直接触发 repair；独立 `ai/rhythm_repair.py` 集中维护 `ai-rhythm-repair-gate-v1`，把经 fixture 校准的静音违规、极端无支持 note 和强 onset 完全无响应写入内容校验错误，并在 sidecar 持久化版本与阈值；provider 请求显式超时并关闭 LiteLLM 内部重试，timeout、连接、HTTP、认证、参数及其他 provider 调用异常共用 transport retry，默认重试 3 次、上限 5 次，transport retry 与内容修复独立；AI 只接受 canonical tick 的 `hits` / `long_notes`，旧 notes 字符串 schema 进入 repair；prompt 明确要求大音符与前后任意普通击打保持超过 0.25 秒真实时间隔离，并把快速 `102` / `1002` 视为只能使用 `1`/`2` 的左右手交替型；`ai/client.py` 会对编码后的跨小节实际时间再次执行同一隔离校验，不合格的 `3`/`4` 进入内容 repair；至少 16 个普通 hit 时，任意 finest-grid 位置同时达到 4 个且超过 10% 也会进入 repair，稳定格点定义为 canonical tick 可被 3 整除的直拍子网格或可被 2 整除的三连音/24 分子网格；至少 16 小节且骨架包含 32 个 hit 的 AI 输出，以及任一直接使用最高 canonical resolution 的局部重生成，会在其余内容校验前把普通 hit 确定性对齐到 `rhythm-skeleton-v5`：按最近 AI hit 保留咚咔顺序，同位置大音符可继续接受隔离校验，移动的大音符降为同色普通 note，可靠 long note 保留并替代其持续范围内的骨架 hit；85% 骨架覆盖率与 15% 骨架外比例只作为对齐后的内部不变量检查，稳定格点校验只统计骨架以外的任意 finest-grid hit，不再要求模型逐点复制长数组，也不移动音频骨架原位置，从而避免骨架约束与任意 finest-grid 校验互相冲突或消耗全部 repair；long note 每小节最多一个，必须由可靠 burst 支持、限制在投影后的 burst 范围内且持续至少 0.25 秒，范围外、过短或无 burst 的内容进入 repair；失败或修复耗尽时统一回退规则生成并产生 `GenerationNotice`。
- **TJA 层**：`tja/chart_validator.py` 在文本渲染前对最终 `TjaChart` 执行纯结构化 preflight，统一检查项目支持字符 `01234578`、1–192 的安全可变 notes 长度、每个 `7` 与正整数 `balloon_counts` 的严格一一对应，以及 `5`/`7` 必须由后续 `8` 闭合；校验会汇总全部可发现问题，writer 不再自动补默认气球计数或忽略多余计数，CLI 与 Web 通过同一 `render_tja()` 入口共享该合法性边界；自动生成用例还会在 preflight 前逐小节检查 notes 长度严格匹配 `ResolutionPlan`，直接 TJA 预览则继续允许格式支持的其他安全长度。`tja/quality.py` 为 AI sanitize 或 fallback 后的最终 `ChartBar` 生成非阻断 `QualityReport`；`quality-report-rhythm-alignment-v1` 集中把 note/onset、strong onset、unsupported note、downbeat、fill burst、rhythmic quantization 和 silent range 七类节奏对齐指标定义为质量主线，AI repair 仍只选择静音违规、极端 unsupported note 和多小节 strong onset 零响应三个已校准门槛，具体 instrument 指标及 structure/resolution/负荷等字段只保留兼容的 report-only 诊断，不作为默认模型升级条件，也不进入统一总分。报告统一记录 density、静音、空段、归一化重复、咚咔、真实时间 NPS/连续流、按拍号限制每小节最高优先级点数的统一 accent salience 候选覆盖、普通 note 对完整 canonical 可靠瞬态的 50 ms 时间容差对齐命中数/评估数/比例（排除特殊音符并支持跨小节匹配）、强瞬态由普通 note 或有效持续区间响应的 50 ms 一对一指标、可靠 downbeat 由普通 note 响应的 70 ms 一对一指标、基于 transient/beat/activity/structure 与非链式 0.3 秒连接的 unsupported note 数/评估数/比例、首尾及曲中确定静音范围的违规小节数/note 数/比例、特殊音符与普通 fill 对可靠 burst/结构 fill candidate 的对齐数/评估数/比例、普通 note 对最近可靠 salience 的 canonical tick 平均量化误差、按 silent/rest/sparse/normal/dense/fill 汇总的可靠 salience 响应计数与 coverage、特殊音符，以及 `structure_density_correlation`、`peak_contrast`、`build_up_slope_agreement`、`cadence_variation_rate`、`fill_candidate_precision`、section motif/return、highlight contrast、drum onset coverage、bass/downbeat alignment、vocal phrase response、instrument transition/confident bar/fill support、base resolution、切换率、高分辨率占比、量化误差和 avoided changes；普通 hit/NPS/连续流/重音/重复均按真实时间或归一化事件位置计算，不随 16/24/48 字符串长度变化，普通负荷只统计 `1`–`4`。rhythm alignment 中只有可靠静音范围违规、极端 unsupported note 和多小节 strong onset 完全无响应通过版本化 `ai-rhythm-repair-gate-v1` 进入 AI 内容 repair；note/onset、downbeat、fill burst、量化误差、density coverage 及结构、instrument、resolution 指标继续 report-only，全部指标均不进入统一加权总分。AI 门控继续独立维护宽松样本下限和阈值，使 validity 与 musical quality 分离。`tja/writer.py` 负责把通过 preflight 的 `TjaChart` 渲染为文本，并提供 `.tja` 文件读写辅助；项目内部 `offset` 表示第一拍/小节起点在音频中的时间，导出 TJA 时按 TJA 语义取反；生成的 `.tja` 默认以 CP932 / Shift-JIS 兼容编码保存，为非 4/4 小节输出 `#MEASURE 3/4` 后再复位，并在存在气球音符时输出 `BALLOON:`；`tja/validator.py` 做 MVP 级文本格式检查。
- **Web 层**：`web.py` 提供本地 FastAPI MVP，支持上传音频、播放转换后的 OGG、手动覆盖 BPM/OFFSET/拍号、全曲生成和按起止小节重新生成；进度页轮询 `progress.json` 展示接收、转换、节拍分析、可选声部节奏增强、生成和写入阶段，并实时渲染结构化 notices，结果页继续持久显示同一批提示。全曲任务写入 `generation_notices.json`，局部 regenerate 写入 `generation_notices_<start>_<end>.json`；分析置信度降级、BeatNet 未生效、`spectral-v1` fallback、`instrument-v1` / `stem-role-v1` complete、classifier-only 降级、旧 partial 与 fallback、基础 resolution 回退、AI fallback 和最终写入错误都不会再静默，元数据重试成功会移除已解决的 render error 并记录成功 notice。任务文件写入 `output/web/<job-id>/`，单个上传最大 100 MiB，全曲生成额外保存 `chart_bars.json` 和 `chart_options.json` 供编码失败后复用；默认只允许回环监听，非回环必须显式 `--allow-remote`，远程模式还需额外 `--allow-instrument-analysis` 才允许请求方触发 Demucs/AST 重型推理，并继续禁用任意服务器目录导出，通用下载端点只公开预览 OGG 和生成的 TJA，不公开 AI sidecar、notice sidecar 或内部状态，并从公开进度/结果中隐藏 notice detail、服务器路径、连接地址和 provider 响应。结果页可在本机复制 `.ogg` / `.tja` 到指定目录并处理覆盖、改名、`.bak` 备份和打开目录；改名会同步更新 `WAVE:`。自定义 `ai_base_url` 必须同时提供本次请求的 `ai_api_key`，两项都为空时只成对使用服务端环境变量；持久化 AI 输出、失败尝试、错误和 notice 前均脱敏 API key。首页和局部 regenerate 可设置 1–600 秒请求超时和 0–5 次 transport retry，局部 AI 调用通过 `asyncio.to_thread()` 避免阻塞事件循环；全曲进度页提供终止按钮和 `/jobs/<job-id>/cancel` 接口，任务使用协作式取消在流水线阶段边界停止，AI provider 调用改用可取消的 LiteLLM `acompletion` task，因此终止请求可直接取消正在等待的 AI HTTP 调用。
- **评测、参考与工具层**：`evaluation/instrument_benchmark.py` 与 `tools/benchmark_instrument_models.py` 执行 `instrument-model-benchmark-v1`，按 profile 校验模型 hash，在阻断网络与强制离线环境中运行真实 instrument analysis，并以 JSON/Markdown report-only 记录模型组件大小、设备、耗时、real-time factor、进程峰值 RSS 和输出证据数量；`evaluation/audio_benchmark.py` 提供 `audio-alignment-v2` 的一对一 onset/beat/downbeat/频带/pickup/fill 匹配、BPM/拍号/first downbeat 误差、ResolutionPlan 可表达性/量化/过度升级指标、跨 fixture 聚合和 Markdown 渲染，`tools/benchmark_audio_fixtures.py` 负责加载 ground truth、调用分析器并原子写入 JSON baseline 与 Markdown 报告；`evaluation/chart_alignment.py` 与 `tools/benchmark_chart_alignment.py` 将规则谱面 note 映射回音频时间轴，对四难度聚合 note/onset、强 onset、downbeat、fill、无证据 note、静音违规和重复生成确定性，同时汇总 QualityReport report-only 指标、按 course/density hint 的 salience coverage 及其与独立 fixture ground truth 的方向性差值，并原子写入 `chart-alignment-v1` JSON/Markdown 基线；`evaluation/phase_zero_acceptance.py` 与 `tools/verify_phase_zero.py` 将 fixture 双重建、禁网双 benchmark、baseline 契约和持久化隐私汇总为可重复的 Phase 0 阶段验收及 JSON/Markdown 报告；`evaluation/phase_one_acceptance.py` 与 `tools/verify_phase_one.py` 将 17 个合成 fixture 的禁网双次 salience 分析汇总为 `phase-one-acceptance-v1`，检查 canonical grid、确定性、首尾静音、onset 峰值对齐、confidence/reason 契约和稀疏输出；`evaluation/phase_two_acceptance.py` 与 `tools/verify_phase_two.py` 执行 `phase-two-acceptance-v1`：在禁用 socket 连接时双跑 17 个 fixture/68 张规则谱面的 `chart-alignment-v1`，对比 Phase 2 前 baseline 的 note/onset、无支持 note、strong/downbeat、静音和 course 单调指标，同时执行 360 配置/1080 小节约束矩阵，并用 AI 实际消费的 `ai-salience-validation-v1` 评估规则结果以锁定共享 report-only 对齐契约；结果写入 `phase_two_acceptance.json` 和 `.md`；`evaluation/phase_three_acceptance.py` 与 `tools/verify_phase_three.py` 执行 `phase-three-acceptance-v1`：禁网双跑同一批 17 个 fixture/68 张谱面并与 Phase 2 验收结果比较 strong onset/downbeat recall，另以确定性行为矩阵覆盖统一 accent/cadence 响应、大音符 course 上限与跨小节 0.25 秒真实时间隔离、低/高频 don/ka 证据响应和最长单色串 report-only 诊断，以及 4/4、3/4、6/8 全 resolution 家族/四 course 的 72 个滚奏与气球配置；真实 `fill_burst_120.wav` 必须只在 ground truth 第 4、8 小节检测并消费可靠 burst，结果写入 `phase_three_acceptance.json` 和 `.md`；`evaluation/phase_four_acceptance.py` 与 `tools/verify_phase_four.py` 执行 `phase-four-acceptance-v1`：禁网双跑稳定 4/4、3/4、6/8、真正弱起、半速/倍速歧义和弱节奏 6 个聚焦 fixture，并以确定性行为矩阵覆盖候选拒绝、完整 BeatNet 胜出、meter/downbeat 部分采用、ambiguity、手动覆盖和变速分类；验收要求 BPM/beat/downbeat/pickup 不退化、half/double tempo 错误为零、默认路径不加载 BeatNet、最终候选唯一且诊断完整，结果写入 `phase_four_acceptance.json` 和 `.md`，当前真实 BeatNet fixture 均被保守拒绝，因此 BeatNet 继续默认关闭；`evaluation/phase_five_acceptance.py` 与 `tools/verify_phase_five.py` 执行 `phase-five-acceptance-v1`：在禁用 socket 连接时双跑确定性模型 profile 准备/hash/离线 benchmark、7 个 stem-role salience 行为场景、`compact-v7` taxonomy 独立性与 Web 远程权限矩阵，验证 stem-role 无 AST、模型失败不改变默认 salience、四类 stem 证据受限生效、AI payload 不依赖具体乐器分类、远程重型分析仍需管理员显式允许，结果写入 `phase_five_acceptance.json` 和 `.md`；`evaluation/phase_six_acceptance.py` 与 `tools/verify_phase_six.py` 执行 `phase-six-acceptance-v1`：在禁用 socket 连接时双跑全部 17 个 fixture / 68 张规则谱面的 `chart-alignment-v1` 与 6 个确定性行为场景，验证规则/AI 共用 QualityReport、八类核心指标完整覆盖、4/4 的 16/24/48 及 3/4/6/8 的 12/18/36 等价编码稳定、ground-truth A/B 校准完整、`ai-rhythm-repair-gate-v1` 仅选择三个极端门槛且规则基线零触发，并确认不存在统一质量总分；结果写入 `phase_six_acceptance.json` 和 `.md`；`evaluation/phase_seven_acceptance.py` 与 `tools/verify_phase_seven.py` 执行 `phase-seven-acceptance-v1`：在禁用 socket 连接时双跑 3 个消费者收敛场景、旧兼容快照和 CLI/Web 能力矩阵，验证真实 `click_4_4.wav` 在不加载 torch/Demucs/Transformers 时可完成转换、分析、structure、fallback 与 QualityReport，stem-role 只受限增强 activity/onset、具体乐器 taxonomy 不改变 structure/fallback/AI payload/QualityReport 主线、compact-v7 instrument projection 比 compact-v4 更小、旧 analysis/config/sidecar 可读，以及默认 profile、动态进度、notice 和远程权限与实际能力一致；结果写入 `phase_seven_acceptance.json` 和 `.md`；`tests/test_phase_two_constraints.py` 以 4/4、3/4、6/8 的全部 resolution 家族、四 course 和五档 density 组成规则生成约束矩阵，统一锁定输出长度、course/density 单调性、真实时间 NPS、occupancy、跨 resolution 等价 hit 数和 silent/rest/sparse 上限；`reference/tja_parser.py`、`reference/benchmark.py`、`reference/dataset.py` 和 `reference/windows.py` 负责离线检查同 stem 音频/TJA、解析多 course、BPMCHANGE、MEASURE、GOGO、可变 resolution，并输出匿名聚合或连续稀疏事件窗口；`tools/analyze_reference_dataset.py` 生成匿名 benchmark，`tools/build_reference_windows.py` 原子生成运行时静态窗口。当前仓库静态窗口由 5 个匿名 source、23 个 course 生成 69 段，只有 source hash、course/level、窗口角色和逐小节紧凑事件；外部音频/TJA、标题、WAVE、绝对路径和完整 notes 不进入仓库，运行时生成流程不访问外部目录。`utils/paths.py` 集中处理 UTF-8 JSON 原子写入和 Pydantic 模型序列化。

`generate` 当前数据流：输入音频 → `generation_config.json` → `output/<stem>.ogg` → librosa onset-grid/可选 BeatNet + `spectral-v1` + 可选本地 `instrument-v1` → canonical `BarFeature` → `structure-v1` / phrase plan → `ResolutionPlan` → `analysis.json` → AI sanitize 或 fallback `ChartBar` → `quality_report*.json` / `generation_notices.json` → 结构化谱面 preflight → 一个或多个 CP932 / Shift-JIS `.tja` → `report.txt`。CLI 单难度写入 `quality_report.json`，全难度按 course 写入 `quality_report_<course>.json`；Web 全曲和局部重生成分别写入 `quality_report_1_<end>.json`、`quality_report_<start>_<end>.json` 与对应 `generation_notices*.json`，这些都是不可通过通用下载端点访问的内部 sidecar，且不包含 API key 或运行时连接信息。`analysis.json` 除最终 BPM/OFFSET 外还记录 `analysis_schema_version=11`、librosa、librosa+onset-grid、BeatNet、BeatNet+onset-grid 的统一 `tempo_candidates`、BeatNet 状态/reason、`spectral-v1` 版本/状态/reason、`instrument-v1` 版本/状态/reason、模型 ID/实际设备、canonical grid 与逐小节频谱/声部/乐器汇总和稀疏攻击、`structure-v1` 逐小节结构、phrase plan、`song-global-v1` 或 `phrase-stable-v2` ResolutionPlan、自动分析来源和 onset-grid 置信度决策；手动 `--bpm` / `--offset` 覆盖会在最终 analyzer 后追加 `manual-override`，但保留覆盖前的自动估计诊断。`generation_config.json` 使用带 `schema_version=1` 的 `GenerationConfig`，记录输入参数、难度、是否生成全难度、风格、density、max-bars、BPM/OFFSET 覆盖、拍号覆盖、BeatNet 开关、阶段 C 开关/profile/设备/可选模型目录、特殊音符开关、AI 开关、最终解析使用的模型名、内容修复次数、单次请求超时和 transport 重试次数；`generate-from-config` 可读取版本 1 配置，并将旧的无版本配置按版本 1 兼容解释，显式未知版本会被拒绝；API key 和 base URL 不是配置字段，复跑 AI 生成时仍需通过 `.env`、环境变量或命令参数提供。AI 环境变量只使用 `MODEL`、`OPENAI_BASE_URL` 和 `OPENAI_API_KEY`。启用 `--use-ai` 时，prompt 默认按 course 和相近 level 从 `ai/reference_windows.json` 选择 intro、peak、cadence 连续窗口，当前静态数据含 5 个匿名 source、23 个 course、69 个窗口；只传递匿名 source ID、course/level、逐小节 resolution/measure/BPM/GOGO 和稀疏事件，运行时不访问外部参考目录。静态窗口缺失或不可读时才回退到 `ai/examples.py` 中 3 首、共 72 个均匀抽样旧参考小节。`--bpm` / `--offset` 会在音频分析后覆盖 raw analysis，并影响小节网格、`analysis.json` 和最终 `.tja`，其中 raw analysis / `analysis.json` 保留内部正向 offset，写入 `.tja` 时按 TJA 播放语义取反；`--time-signature 4/4|3/4|6/8` 会覆盖 raw analysis 的拍号，BPM 始终按四分音符解释，3/4 和 6/8 在分析中使用 36 canonical tick、由全曲 `ResolutionPlan` 在 12/18/36 格中稳定选择输出并在 `.tja` 中输出 `#MEASURE 3/4`，但 3/4 使用 canonical `0/12/24` 三个主拍而 6/8 使用 `0/18` 两个复拍子主拍；`--use-beatnet` 会在音频分析阶段尝试记录 downbeat、beat number、meter 和更准 offset，失败时自动沿用 librosa；小节特征会把 onset、归一化 RMS activity、`spectral-v1` frame 和可选 `instrument-v1` stem/window 映射到格点/小节，并持久化每小节绝对/相对 RMS、峰值、持续帧比例、频带 onset/flux/brightness/novelty/percussive balance，以及供 AI prompt 使用的每格瞬态强弱、持续音乐活动、稀疏 spectral attack、基于拍号网格的 beat/downbeat、乐句位置和 fill 候选信息；`--special-notes` 允许规则生成器和 AI 使用 5/7/8 组成简单滚奏和气球，writer 会为气球输出 `BALLOON:`；`--max-bars N` 会在小节特征阶段只保留前 N 小节，便于用真实音频快速验证；`--density auto|low|medium|high|max` 控制规则生成器密度，并在启用 AI 时写入 prompt payload。启用 `--use-ai` 时，AI 输出必须通过 JSON、bar 数量、canonical tick 类型/范围、目标 resolution 可表示性、事件冲突、长音结构以及 high/max/Oni 高难谱面的密度、多样性和中段空小节质量门控；质量门控会按每小节 density hint 排除真实 rest/sparse 停顿，并按 hint 的 min/max 范围限制 normal/dense/fill 小节，避免普通段落过稀和局部突然满密；识别为歌曲开头或结尾连续静音的小节必须输出全 0；首尾低绝对/相对响度、低持续比例且只有少量 onset 的孤立毛刺也属于该统一静音边界，AI 不合格时会进入修复重试，清洗阶段也会再次把这些小节置空；不通过的尝试会把校验原因、AI 原始输出和最终结果写入同目录的 `ai_attempts*.json`；成功结果的 `ai_output*.json` 以及需要写入的 attempt sidecar 同时保存 report-only `salience_validation`，用于后续比较可靠候选覆盖、强瞬态响应、无支持 note、静音和可表达性；该文件同时以独立 `transport_attempts` 记录每次真实 provider 调用的耗时、异常类别和重试序号，并在最终失败时写入稳定的 `fallback_reason`，所有错误在持久化前脱敏 API key。不合格内容默认最多自动修复重试 2 次；transport 失败不消耗该次数，任一边界耗尽后记录 `ai_output.json` 并回退到规则生成器。Web UI 数据流：上传音频 → `output/web/<job-id>/` 输入文件 → 后台任务写入并持续更新 `progress.json`，进度页轮询 `/jobs/<job-id>/status` 展示接收音频、转换 OGG、分析节拍、可选识别人声/乐器、生成谱面和写入预览阶段 → `.ogg` → `analysis.json` → 按首页选择的难度、风格、密度、特殊音符和可选 AI 增强生成全曲 `ChartBar`，并保存 `chart_bars.json` / `chart_options.json` 供失败恢复 → 写入 CP932 / Shift-JIS 编码的 `preview.tja`（首页 AI 写入 `ai_input_1_<end>.json` / `ai_output_1_<end>.json`，若有不通过尝试则额外写入 `ai_attempts_1_<end>.json`，失败时自动回退规则生成且不持久化 API key）→ 若写入阶段仅因 TJA 元数据编码失败，进度页可直接修改歌名/歌手并复用已保存 `ChartBar` 重试写入预览，不重新分析音频也不重新调用 AI → 任务完成后将结果页缓存为 `result.html` 并从进度页跳转到 `/jobs/<job-id>/result` → 结果页仍可局部 regenerate `.tja`（写入 `ai_input_<start>_<end>.json` / `ai_output_<start>_<end>.json`）→ 生成完成页以游玩轨道和底部 timeline 可视化展示谱面，播放按钮驱动 OGG 自动演奏，range 进度条可跳转查看任意位置，咚/咔命中位置会从内置 `/assets/taiko_don_16bit_44100.wav` 和 `/assets/taiko_ka_16bit_44100.wav` 播放打击音效；结果页的保存按钮会把当前预览对应的 `.ogg` 和 `.tja` 复制到用户填写的目录，并把 `.tja` 重命名为与 `.ogg` 同 stem；目标文件已存在时先进入冲突页，可显式覆盖、改名保存、备份旧文件后保存或打开目标目录，改名保存时导出 TJA 的 `WAVE:` 同步指向新 OGG 文件名；直接播放 TJA 调试入口只接受已准备好的 `.ogg`，不再调用 ffmpeg 转换。

骨架密度约束：AI 普通击打及长音替代范围与内部 musical skeleton 完全一致时，最低密度、平均密度与连续空小节的补点要求让位于骨架；密度上限、静音、可玩性和独立音频质量门控继续执行。不完全匹配的结果仍执行原有最低密度检查。该调整只影响 AI musical skeleton 路径，基础音频 BPM/OFFSET 分析和独立规则生成器不变。

## 工程约束

- `.tja` 生成不要只依赖自由文本拼接；应尽早建立结构化谱面表示，再由导出器负责格式化。
- 音频分析结果、AI 输入、AI 原始输出、校验结果和最终 `.tja` 应尽量可追踪，便于比较不同版本生成质量。
- AI 生成逻辑应保留 prompt / 参数 / 模型版本 / base URL / 输出尝试记录等信息，以便复现实验结果；不要把 API key 写入输出文件。
- 新增或调整 AI prompt / payload 内容时，必须在保证现有功能、规则约束和校验语义完整的前提下尽量精简；优先使用结构化字段、紧凑数组和 legend 表达重复信息，避免把可结构化内容展开成冗长自然语言或重复长字段名。
- 对外部音频分析工具或 AI SDK 的调用应集中封装，避免散落在业务流程中。
- 新增核心模块时，同时考虑其输入、输出、错误模型和可测试边界。
- 只有在任务确实需要独立并行探索、跨大量文件调查或专业能力时才派子代理；普通实现、审查和验证优先由主代理直接完成，不要为了流程形式批量派出子代理浪费 token。
- 如果引入临时脚本，应明确其生命周期；稳定后迁移到正式模块或从仓库删除。
- 每次代码修改完成后，基于 git 工作树中的所有更改给出一份用于 git 提交的完整日志。
- Git 提交信息使用中文，详细描述本次变更的目标或结果。
- Git 提交文件要遵循 `.gitignore` 规则，不要绕过，如有问题，先进行提醒或询问。

## AGENTS.md 维护要求

在修改项目代码时，如果出现以下变化，必须更新本文件：

- 新增或变更常用开发命令。
- 新增技术栈、框架、包管理器或测试框架。
- 新增核心目录、流水线阶段或架构边界。
- 改变音频分析、AI 生成、校验或 `.tja` 导出的数据流。
- 引入必须安装的本地工具、系统依赖或外部服务配置。
- 确立新的工程约定、文件命名规则或持久化格式。

更新时只记录对未来维护者有帮助的信息，避免写入临时计划、已经能从代码中直接看出的文件清单，或泛泛的编码建议。
